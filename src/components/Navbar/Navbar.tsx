import React, { FC } from 'react';

interface INavbar {
    setHeight: (height: number) => void;
}

const Navbar: FC<INavbar> = ({setHeight}) => {
    return (
        <div>

        </div>
    );
};

export default Navbar;